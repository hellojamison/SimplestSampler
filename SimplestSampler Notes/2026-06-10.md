# 2026-06-10

- 02:30: Fix choppy window resize. Working: `WindowFrameTracker` applies saved frame once (not on every SwiftUI update); debounced frame persistence (300ms); skip redundant `windowFrame` saves; `xcodebuild` BUILD SUCCEEDED. Not verified: live resize UX.
- 02:15: Active tab drag-to-reorder. Working: grip handle on Active rows; `moveActiveSlot(from:to:)` persists `recentCaptures` order in session state; file drop unchanged; `xcodebuild` BUILD SUCCEEDED. Not verified: live drag UX. → [[2026-06-11-active-slot-reorder]]
- 02:00: Rename field highlight visibility. Working: `SlotRowView` rename uses theme `renameFieldFill`/`renameFieldBorder` (white field, dark text, 2px accent border, shadow); hides macOS 14+ default focus ring when editing; select-all on focus; `xcodebuild` BUILD SUCCEEDED. Not verified: live rename on Active/Stored/Simple-ish in light + dark. → [[2026-06-10-simpleish-tab]]
- 01:45: PT capture on PT 26.4. Working: user confirmed direct capture works after session-export + GetClipList fix (no consolidate). → [[2026-06-06-phase-2-pt-capture]]
- 01:15: Session export + GetClipList direct resolve. Working: native `ptsl_markers_helper` prefers `session_export` segments over playlist rows when export has `source_start/end` (even equal segment counts); `--get-selected-clip-file` enriches modern PT with session-export playback window; Swift uses `source_start_seconds`/`source_end_seconds` on clip file; PT 25+ consolidate blocked again; helper rebuilt (`node scripts/build-native-helper.js`); `xcodebuild` BUILD SUCCEEDED. Not verified: live capture on user session. → [[2026-06-06-phase-2-pt-capture]]
- 00:30: PT capture parity when direct resolve fails. Working: restore OverCue consolidate backup on PT 25+ (`shouldAvoidConsolidateFallback` = release < 25); enrich clip via `--resolve-clip-start-time-by-id`; prefer segment source windows before clip math; clearer errors when clip file exists; `xcodebuild` BUILD SUCCEEDED. Not verified: live capture on user session. → [[2026-06-06-phase-2-pt-capture]]
- 00:15: Align PT direct resolve with OverCue. Working: `resolveSourceFromSelection` uses session fps fallback chain (clip → segments → timeline), any-segment source window fallback, OverCue-style anchor tc + reject-reason debug; link-track hint when range has no clip; `xcodebuild` BUILD SUCCEEDED. Not verified: live capture on user session. → [[2026-06-06-phase-2-pt-capture]]
- 21:00: Fix modern PT wrongly using consolidate fallback. Working: `PTTimecodeMath.ptslReleaseMajor` normalizes `2025.x` → `25`; `shouldAvoidConsolidateFallback` now true for release ≥ 25 (was inverted / compared raw 2025); unknown protocol defaults to no consolidate; capture debug logs release major; `xcodebuild` BUILD SUCCEEDED. Not verified: live PT capture on 2025.x session. → [[2026-06-06-phase-2-pt-capture]]
- 22:00: Rename commits on click-away. Working: `SlotRowView` row tap while editing now calls `dismissRenameField()` (was no-op `guard !nameFocused` that swallowed tap without blur); selection-change + row action buttons also dismiss so `onChange(of: nameFocused)` runs `commitRename()`; `xcodebuild` BUILD SUCCEEDED. Not verified: live single-click rename on Active rows and double-click rename on Stored/Simple-ish. → [[2026-06-10-simpleish-tab]]
- 20:00: Fix stored rename not sticking. Working: `SlotRowView` always binds TextField to `draftName` (avoids focus-loss revert to stale `capture.slotLabel`); sync from model when not editing; skip no-op commits; `xcodebuild` BUILD SUCCEEDED. Not verified: live double-click rename on Simple-ish/Stored. → [[2026-06-10-simpleish-tab]]
- 19:00: Drag samples onto category chips (Simple-ish). Working: `SlotRowView.onDrag` on Simple-ish rows; `CategoryFilterChip.onDrop` on Uncategorized + user categories with highlight; `xcodebuild` BUILD SUCCEEDED. Not verified: live drag UX. → [[2026-06-10-simpleish-tab]]
- 18:00: Simple-ish tab with categories. Working: third tab lists stored samples with filter chips (All / Uncategorized / user categories), per-row category menu, categories persisted in `.simplestsampler-library.json`; `xcodebuild` BUILD SUCCEEDED. Not verified: live category CRUD and filter UX. → [[2026-06-10-simpleish-tab]]
- 23:30: Fix volume meter not animating. Working: forward `audioPlayback.objectWillChange` through `SamplerViewModel`; tap `playerNode` (not mixer) with Int16 peak support; hide meter when idle; `xcodebuild` BUILD SUCCEEDED. Not verified: live play.
- 16:00: Clear slot label after delete. Working: `SlotRowView` name binding reads from `capture` when unfocused (fixes stale `@State` on index-stable rows); `deleteActiveSlot` clears selection when deleted; `xcodebuild` BUILD SUCCEEDED. Not verified: live delete.
- 15:00: Volume output meter under slider. Working: mixer tap → `outputLevel`; `VolumeMeterView` capsule bar aligned with slider; `xcodebuild` BUILD SUCCEEDED. Not verified: live level response.
- 14:00: Slot playback progress highlight. Working: `AudioPlaybackService.playbackProgress` (0…1); `SlotRowView` left-fill bg bar while playing; `xcodebuild` BUILD SUCCEEDED. Not verified: live play scrub.
- 12:00: Fix edit-range capture inside long clips. Working: `ProToolsCaptureService` now matches OverCue — sole segment only (`segments.count == 1`), clip trim math uses `selectedClip.sessionFps` only, no full-file fallback when `hasEditRange`, PT 25+ consolidate fallback when direct resolve returns nil; `xcodebuild` BUILD SUCCEEDED. Not verified: live PT re-test (small selection in long clip). → [[2026-06-06-phase-2-pt-capture]]
