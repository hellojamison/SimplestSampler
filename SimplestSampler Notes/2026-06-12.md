# 2026-06-12

- 23:55: AAX GUI timer crash fix (fourth pass). **Working:** `otool -tv` mapped `-[SimplestSamplerPanelView refreshOnMainThread] + 132` to `[_slotRows copy]` inside `@synchronized` — root cause is `performSelectorOnMainThread` messaging a deallocating panel under MRC (pass 3 `@synchronized`/cancel/nil-subviews insufficient); replaced timer refresh with `SchedulePanelRefresh()` (`dispatch_async` + retain/release + `refreshEpoch` stale-block guard), immutable `_slotRowsSnapshot` at init (no runtime copy), removed detach nil of subviews, `dealloc`/`detachFromGUI` bump epoch. **Verified:** `node scripts/build-aax-plugin.js` → `** BUILD SUCCEEDED **`; installed to `/Library/Application Support/Avid/Audio/Plug-Ins/SimplestSamplerAudioSuite.aaxplugin`; binary mtime `Jun 12 23:55:15 2026`; post-fix disassembly shows `refreshOnMainThread` no longer calls `copy` or `objc_sync_enter`. **Not verified:** Pro Tools relaunch/rescan without crash, AudioSuite panel render, capture round-trip, repeated open/close cycles. **Tried and failed:** pass 1 (detach/nil guards), pass 2 (`PanelView()` + `mPanelRefreshEnabled`), pass 3 (`@synchronized` + `refreshOnMainThread` + cancel selectors) — user confirmed PT still crashed on load. → [[2026-06-12-aax-gui-crash-fix]] **Working:** `otool`/`atos` mapped crash offset `+352` in `-[SimplestSamplerPanelView refresh]` to `[_slotRows copy]` at line 258 — `_slotRows` use-after-free when `TimerWakeup`/`performSelectorOnMainThread` races view teardown; added `refreshOnMainThread` with `@synchronized` snapshot of popup/rows/gui, `cancelPreviousPerformRequests` + nil subview refs on detach, and main-thread-only timer refresh gated on `readyForRefresh`. **Verified:** `node scripts/build-aax-plugin.js` → `** BUILD SUCCEEDED **`; reinstalled to `/Library/Application Support/Avid/Audio/Plug-Ins/SimplestSamplerAudioSuite.aaxplugin`. **Not verified:** Pro Tools relaunch/rescan without crash, AudioSuite panel render, capture round-trip. **Tried and failed:** pass 1 (detach/nil guards) and pass 2 (`PanelView()` + `mPanelRefreshEnabled`) — user confirmed PT still crashed on load. → [[2026-06-12-aax-gui-crash-fix]]
- 23:32: AAX GUI timer crash fix (second pass). **Working:** removed stale raw `mPanelView` pointer; `PanelView()` now resolves the panel only through live `mViewController.view`; added `mPanelRefreshEnabled` gate so `TimerWakeup`/`RefreshPanel` skip until the panel is in the view hierarchy; panel gets `readyForRefresh` only after init completes and clears on detach/dealloc; `refresh` copies slot rows, validates class, and uses MRC-safe main-thread scheduling. **Verified:** `node scripts/build-aax-plugin.js` → AAX SDK + `SimplestSamplerAudioSuite` `** BUILD SUCCEEDED **`; reinstalled to `/Library/Application Support/Avid/Audio/Plug-Ins/SimplestSamplerAudioSuite.aaxplugin`; binary mtime `Jun 12 23:32:36 2026`. **Not verified:** Pro Tools relaunch/rescan without `EXC_BAD_ACCESS`, AudioSuite slot panel render, capture round-trip. **Tried and failed:** first-pass fix (detach + nil guards) still crashed PT on load with `objc_msgSend` in `-[SimplestSamplerPanelView refresh]` from `TimerWakeup`. → [[2026-06-12-aax-gui-crash-fix]]
- 23:16: Fixed AAX GUI timer use-after-free on Pro Tools load. **Working:** `SimplestSampler_AS_GUI.mm` now detaches `SimplestSamplerPanelView` before `DeleteViewContainer`, nils `mPanelView`, guards `TimerWakeup`/`refresh` when the view container or GUI back-pointer is gone, and recreates panel contents after teardown; `node scripts/build-aax-plugin.js` → AAX SDK + `SimplestSamplerAudioSuite` `** BUILD SUCCEEDED **`; plugin reinstalled to `/Library/Application Support/Avid/Audio/Plug-Ins/SimplestSamplerAudioSuite.aaxplugin`; binary mtime `Jun 12 23:16:05 2026`. **Not verified:** Pro Tools relaunch/rescan without `EXC_BAD_ACCESS`, live AudioSuite slot panel, capture round-trip. **Tried and failed:** none. → [[2026-06-12-aax-gui-crash-fix]]
- 20:52: Rebuilt and reinstalled the AudioSuite plugin with `node scripts/build-aax-plugin.js`. **Working:** AAX SDK and `SimplestSamplerAudioSuite` both reported `** BUILD SUCCEEDED **`; the bundle was reinstalled to `/Library/Application Support/Avid/Audio/Plug-Ins/SimplestSamplerAudioSuite.aaxplugin`; current bridge contract is still aligned because `PluginBridgeService.swift` exports `activeSlotCount` + `shortcutBindings` and `aax/SimplestSamplerAudioSuite/Source/PluginLibraryBridge.cpp` reads the same JSON shape for the Cocoa slot UI. **Verified:** `node scripts/build-aax-plugin.js`; `codesign -dv --verbose=2 "/Library/Application Support/Avid/Audio/Plug-Ins/SimplestSamplerAudioSuite.aaxplugin"` -> ad-hoc signed `com.hellojamison.simplestsampler.audiosuite`; `stat -f "%Sm %N" "/Library/Application Support/Avid/Audio/Plug-Ins/SimplestSamplerAudioSuite.aaxplugin/Contents/MacOS/SimplestSamplerAudioSuite"` -> `Jun 12 20:51:45 2026`. **Not verified:** Pro Tools relaunch/rescan, live AudioSuite GUI check, and capture round-trip back into the app. **Tried and failed:** none. -> [[2026-06-12-aax-plugin-rebuild]]
- 02:41: Sound Board pad drag no longer duplicates clips on pad-to-pad drop. **Working:** `SamplerCapture.swift` no longer treats generic plain-text drags as Active-slot payloads, `SoundBoardView.swift` now emits pad drags with soundboard-only UTTypes instead of an `NSString` plain-text provider, and `SamplerViewModel.swift` now deduplicates `soundboardPads` IDs after move/load/assign/capture writes so one pad capture ID cannot survive on multiple pads. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **` at 2026-06-12 02:41:28; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual Sound Board pad-to-pad drag in the running app for move-to-empty, swap-filled, and cross-check that Active-slot drop/file-drop/capture-to-pad still behave normally. **Tried and failed:** none. -> [[2026-06-12-sound-board-tab]]
- 02:00: Launch-height regression follow-up. **Working:** `SamplerCapture.swift` now sizes Active-tab launch height from explicit slot-row height + inter-row spacing instead of the stale `slotRowStride = 42` undercount, so the default 4-slot Active launch grew from `468` to `516` points and shows the `+ Add Slot` control again; `ContentView.swift` `WindowFrameTracker` and `SamplerViewModel.resizeWindowForCurrentTab()` now compare against current content height before growing the window. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; live relaunch of `build/Build/Products/Debug/SimplestSampler.app`; window bounds via `swift /tmp/list_windows.swift` moved from `612x468` before the patch to `612x516` after the patch; `screencapture -x -l<window-id> /tmp/simplestsampler-window-after.png` shows all 4 Active slots plus the `+ Add Slot` row visible. **Not verified:** live Sound Board relaunch height in the running app, Stored-tab relaunch height with a populated library, and save/reload of the newly grown frame into `samplerSessionState.windowFrame`. **Tried and failed:** none. -> [[2026-06-12-window-sizing-scroll]]
- 02:30: Stored category drag regression follow-up. **Working:** `StoredCategoryBar.swift` now decodes plain-text stored-capture drags before falling back to active-slot parsing, restoring row-to-category chip assignment after the category UI moved into `Stored`; active-slot drops onto category chips still store and assign through the same handler. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **` at 2026-06-12 02:30:23; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual stored-row drag onto `Uncategorized` / named category chips in the running app, and manual Active → Stored category-chip drop after hover-switching tabs. **Tried and failed:** none. -> [[2026-06-12-sound-board-tab]]
- 01:49: Main window launch sizing/scroll pass. **Working:** `SamplerCapture.swift` now makes `defaultWindowHeight` match the default Active-tab preferred height (including slot-count controls) and gives `Stored` extra preferred height for the category bar; `ContentView.swift` now forces the main tab body to fill the available height and wraps `Active` / `Stored` / `Sound Board` bodies in explicit vertical `ScrollView`s with indicators; `WindowFrameTracker` now bumps undersized saved frames up to the current tab's preferred height on launch; `SamplerViewModel.resizeWindowForCurrentTab()` now only grows windows that are too short instead of shrinking taller user-sized frames. **Verified:** `./scripts/build-app.sh` -> `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual relaunch with a previously short saved frame, manual overflow scrolling on all three tabs, and live scrollbar behavior under the current macOS scrollbar preference. **Tried and failed:** none. -> [[2026-06-12-window-sizing-scroll]]
- 01:32: Active slot rows no longer show the left-side drag-grip icon, so the play button is now the leftmost control in `SlotRowView.swift`; full-row drag/reorder and cross-tab drops remain attached to the row-level drag modifier for filled Active slots. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual Active-row interaction balance in the running app after removing the grip, especially play clicks, full-row drag/reorder, Active → Stored / Sound Board drops, inline rename, and Store/Delete/shortcut actions. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
- 01:28: Play controls now use green accent styling with play/stop icons: `ContentView.swift` toolbar play button shows `play.fill` / `stop.fill` on the accent gradient via `SamplerToolbarButtonStyle`, and `SlotRowView.swift` row play buttons keep the green accent fill with white play/stop glyphs (selected rows stay inverted for contrast). **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual toolbar and row play/stop interaction in the running app. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
- 01:26: Restored the main `Active` / `Stored` / `Sound Board` tab bar to a compact centered pill after the hit-area pass stretched it full width: `ContentView.swift` removes the per-tab `.frame(maxWidth: .infinity)` expansion, keeps each tab at intrinsic width, and preserves the larger click target by leaving the horizontal tab padding + `contentShape(Rectangle())` inside each pill while drag-hover tab switching remains in place. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual mouse hit-testing and active-slot drag-hover/drop behavior in the running app with the restored compact pill layout. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
- 01:08: Sound Board pads can be rearranged by drag: `SoundBoardView.swift` emits a soundboard-pad drag payload from filled pads, and dropping onto another pad calls `SamplerViewModel.moveSoundboardPad(from:to:)` which moves into an empty pad or swaps when both are filled. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual pad-to-pad drag in the running app alongside click-to-play, file drop, Active-slot drop, and context-menu clear. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
- 01:04: Main tab pills now use full-width click targets instead of label-only hit areas: `ContentView.swift` gives each `Active` / `Stored` / `Sound Board` tab an equal-width segment and applies a full-rect content shape so clicking anywhere inside the visible segment switches tabs, while the existing pill styling and drag-hover tab switching stay in place. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual mouse hit-testing and active-slot drag-hover/drop behavior in the running app after widening the tab segments. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
- 00:38: Active-slot drag/drop now crosses tabs: `SlotRowView.swift` emits an active-slot drag payload from the grip, Active reorder still lands on row drops, `ContentView.swift` tab pills switch to `Stored` / `Sound Board` while dragging, `StoredCategoryBar.swift` stores dragged Active clips (and assigns category on chip drop), and `SoundBoardView.swift` assigns dragged Active clips onto pads without duplicating the source file. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual drag hover/drop behavior in the running app across Active ↔ Stored / Sound Board. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
- 01:02: Active-slot rows with clips now start drag from the full row instead of only the grip: `SlotRowView.swift` moves the Active drag source to a row-level modifier while keeping row selection, rename focus, button actions, and row drop handling in place; Stored rows keep their existing whole-row drag behavior. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual interaction conflicts in the running app, especially clicking play/store/delete/shortcuts, inline rename, file drop onto a row, and Active drag/reorder or Active → Stored / Sound Board drag from the name/file area. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
- 00:16: Sound Board tab (4×4 pads). Working: added `soundboard` tab with 16-pad grid in `SoundBoardView.swift`; session persistence via `SamplerSessionState.soundboardPads`; pad play, audio drop, PT capture targeting, and context-menu clear wired in `SamplerViewModel`. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`. **Not verified:** live pad interaction in app. → [[2026-06-12-sound-board-tab]]
- 00:03: Preferences window sizing pass. Working: `SimplestSampler/Views/ShortcutPreferencesView.swift` now uses a compact `minWidth: 392` / `idealWidth: 404` layout, tighter shortcut rows, and content-driven vertical sizing; `SimplestSampler/SimplestSamplerApp.swift` applies `.windowResizability(.contentSize)` to `Settings` so the prefs window opens to its content instead of a roomy default frame. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual visual check of the reopened Settings window in Light and Dark. **Tried and failed:** none. → [[2026-06-06-shortcut-preferences]]
- 00:16: Stored categories moved into the main Stored tab and the visible `Simple-ish` tab was removed; `SimplestSampler/Views/ContentView.swift` now exposes `Active` / `Stored` / `Sound Board`, `SimplestSampler/Views/StoredCategoryBar.swift` owns the category chips + add/rename/delete/drop targets, and `SimplestSampler/ViewModels/SamplerViewModel.swift` now filters Stored rows via `filteredStoredCaptures` / `storedCategoryFilter` while migrating saved `simpleish` sessions to `stored`. **Verified:** `./scripts/build-app.sh` → `** BUILD SUCCEEDED **`; app bundle at `build/Build/Products/Debug/SimplestSampler.app`. **Not verified:** manual category drag/filter/picker flows in the running app, and Sound Board tab interaction after the tab-bar cleanup. **Tried and failed:** none. → [[2026-06-12-sound-board-tab]]
